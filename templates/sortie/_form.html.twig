{{ form_start(form) }}
    <div class="text-center" style="text-color-white">
        {{ form_label(form) }}
    </div>
    <div>
        {{ form_widget(form) }}
    </div>
    <button type="button" class="btn btn-light">{{ button_label|default('Organiser') }}</button>
{{ form_end(form) }}
