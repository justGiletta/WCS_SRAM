{% extends 'base.html.twig' %}

{% block title %}Modifier la sortie{% endblock %}

{% block navbar %}{% include '_navbar.html.twig' %}{% endblock %}

{% block body %}
    <h1 class="text-center">Modifier la sortie {{ sortie.name }}</h1>

    <div class="container">
    {{ include('sortie/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="d-flex justify-content-end mt-5">
        <a href="{{ path('sortie_index') }}"><button type="button" class="btn btn-warning">Revenir à l'agenda</button></a>
    </div>

    {{ include('sortie/_delete_form.html.twig') }}
{% endblock %}
